---
apiVersion: getambassador.io/v2
kind: Mapping
metadata:
  name: treetracker-query-api
spec:
  cors:
    origins:
      - '*'
    headers:
      - Content-Type
      - Authorization
    methods:
      - 'GET, POST, PATCH, PUT, OPTIONS, DELETE'
