apiVersion: v1
kind: Service
metadata:
  name: treetracker-query-api
spec:
  selector:
    app: treetracker-query-api
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 3006
